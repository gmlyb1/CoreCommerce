<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.CoreCommerce.repository.WishlistRepository">
	
	<select id="getWishlist" resultType="com.CoreCommerce.domain.Product">
	    SELECT 
	    	 p.id as id
	    	,p.name as name 
	    	,p.price as price
	    	,p.stock as stock
	    	,p.description as description
	    	,p.offerYn as offerYn
	    	,p.image_url as imageUrl
	    	,p.created_at as createdAt
	    	FROM wishlist w
	    JOIN product p ON w.product_id = p.id
	    	WHERE w.member_id = #{memberId}
	    	ORDER BY w.id DESC
	</select>
	
	<!-- 찜 존재 여부 -->
	<select id="existsWishlist" resultType="int">
	    SELECT COUNT(*)
	    FROM wishlist
	    WHERE member_id = #{memberId}
	    AND product_id = #{productId}
	</select>
	
	<!-- 찜 추가 -->
	<insert id="insertWishlist">
	    INSERT INTO wishlist(member_id, product_id)
	    VALUES(#{memberId}, #{productId})
	</insert>
	
	<!-- 찜 삭제 -->
	<delete id="deleteWishlist">
	    DELETE FROM wishlist
	    WHERE member_id = #{memberId}
	    AND product_id = #{productId}
	</delete>

</mapper>