<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.CoreCommerce.repository.NotificationRepository">
	
	 <!-- 1️⃣ 알림 저장 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (user_id, type, content, link, created_at, is_read)
        VALUES (#{userId}, #{type}, #{content}, #{link}, #{createdAt}, #{read})
    </insert>

     <!-- 전체 알림 조회 -->
    <select id="findByUserIdOrderByCreatedAtDesc" resultType="com.CoreCommerce.domain.Notification">
        SELECT * 
        FROM notification
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 타입별 알림 조회 -->
    <select id="findByUserIdAndTypeOrderByCreatedAtDesc" resultType="com.CoreCommerce.domain.Notification">
        SELECT * 
        FROM notification
        WHERE user_id = #{userId} AND type = #{type}
        ORDER BY created_at DESC
    </select>

    <!-- 읽음 처리 -->
    <update id="markAsReadByType">
        UPDATE notification
        SET is_read = true
        WHERE user_id = #{userId}
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
    </update>
	
</mapper>