<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.CoreCommerce.repository.ChatRepository">
	
	<!-- 1. 메시지 저장 -->
    <insert id="insertMessage" parameterType="com.CoreCommerce.domain.ChatMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (room_id, sender, message, created_at)
        VALUES (#{roomId}, #{sender}, #{message}, #{createdAt})
    </insert>

    <!-- 2. 특정 채팅방 메시지 목록 -->
    <select id="findByRoomId" parameterType="long" resultType="com.CoreCommerce.domain.ChatMessage">
        SELECT 
        	 id as id
        	,room_id as roomId
        	,sender as sender
        	,message as message
        	,created_at as createdAt
        FROM chat_message
        WHERE room_id = #{roomId}
        ORDER BY created_at ASC
    </select>
	
	<delete id="deleteByRoomId" parameterType="long">
	    DELETE FROM chat_message
	    WHERE room_id = #{roomId}
	</delete>
	
</mapper>